Student Management API Postman tests

- Collection: collections/student-management-api.postman_collection.json
- Environment: env.postman_environment.json
- Files: files/sample.csv

Run locally (from this folder):

```bash
npm ci
npm test
```

Outputs are written to `reports/report.html` and `reports/junit.xml`.
